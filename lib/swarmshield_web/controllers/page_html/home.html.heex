<Layouts.flash_group flash={@flash} />

<%!-- ============================================================
     FIXED HEADER / NAV
     ============================================================ --%>
<header
  id="landing-header"
  class="fixed top-0 left-0 right-0 z-50 bg-base-100/80 backdrop-blur-md border-b-[0.5px] border-base-300"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <%!-- Logo --%>
      <a href="#" class="flex items-center gap-2.5">
        <div class="flex items-center justify-center size-9 rounded-lg bg-primary/15">
          <.icon name="hero-shield-check-solid" class="size-5 text-primary" />
        </div>
        <span class="text-xl font-bold tracking-tight">SwarmShield</span>
      </a>

      <%!-- Desktop nav links --%>
      <nav class="hidden md:flex items-center gap-8">
        <a href="#features" class="text-sm font-medium text-base-content/70 hover:text-base-content transition-colors">Features</a>
        <a href="#how-it-works" class="text-sm font-medium text-base-content/70 hover:text-base-content transition-colors">How It Works</a>
        <a href="#pricing" class="text-sm font-medium text-base-content/70 hover:text-base-content transition-colors">Pricing</a>
      </nav>

      <%!-- Desktop actions --%>
      <div class="hidden md:flex items-center gap-3">
        <Layouts.theme_toggle />
        <a href={~p"/users/log-in"} class="btn btn-ghost btn-sm h-[36px] text-sm font-medium">
          Log in
        </a>
        <a href={~p"/users/register"} class="btn btn-primary btn-sm h-[36px] text-sm font-medium">
          Get Started
        </a>
      </div>

      <%!-- Mobile hamburger --%>
      <div class="flex md:hidden items-center gap-2">
        <Layouts.theme_toggle />
        <button
          id="landing-mobile-toggle"
          type="button"
          class="p-2 rounded-lg hover:bg-base-200 transition-colors"
          aria-label="Open menu"
        >
          <.icon name="hero-bars-3" class="size-6" />
        </button>
      </div>
    </div>
  </div>

  <%!-- Mobile menu (hidden by default) --%>
  <div id="landing-mobile-menu" class="hidden md:hidden border-t-[0.5px] border-base-300 bg-base-100">
    <div class="px-4 py-4 space-y-3">
      <a href="#features" class="block text-sm font-medium text-base-content/70 hover:text-base-content py-2">Features</a>
      <a href="#how-it-works" class="block text-sm font-medium text-base-content/70 hover:text-base-content py-2">How It Works</a>
      <a href="#pricing" class="block text-sm font-medium text-base-content/70 hover:text-base-content py-2">Pricing</a>
      <div class="pt-3 border-t-[0.5px] border-base-300 flex gap-3">
        <a href={~p"/users/log-in"} class="btn btn-ghost btn-sm flex-1 h-[44px]">Log in</a>
        <a href={~p"/users/register"} class="btn btn-primary btn-sm flex-1 h-[44px]">Get Started</a>
      </div>
    </div>
  </div>
</header>

<%!-- ============================================================
     HERO SECTION
     ============================================================ --%>
<section class="relative min-h-screen flex items-center pt-16 overflow-hidden dot-grid-bg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 w-full py-16 sm:py-20 lg:py-24">
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 lg:gap-16 items-center">
      <%!-- Left: Copy --%>
      <div class="animate-slide-in-left">
        <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-primary/10 border-[0.5px] border-primary/20 mb-6">
          <.icon name="hero-shield-check-mini" class="size-4 text-primary" />
          <span class="text-xs font-semibold text-primary tracking-wide uppercase">AI Agent Firewall</span>
        </div>

        <h1 class="text-4xl sm:text-5xl lg:text-6xl xl:text-7xl font-extrabold tracking-tight leading-[1.1]">
          Your AI Agents.
          <br />
          <span class="gradient-text">Under Control.</span>
        </h1>

        <p class="mt-6 text-lg sm:text-xl text-base-content/70 max-w-xl leading-relaxed">
          The real-time firewall for autonomous AI agents. Policy enforcement in sub-milliseconds,
          multi-agent deliberation for edge cases, and cryptographic data shredding when sessions end.
        </p>

        <div class="mt-8 flex flex-col sm:flex-row gap-4">
          <a href={~p"/users/register"} class="btn btn-primary h-[48px] px-8 text-base font-semibold">
            Get Started Free
            <.icon name="hero-arrow-right-mini" class="size-5 ml-1" />
          </a>
          <a href="#how-it-works" class="btn btn-outline h-[48px] px-8 text-base font-semibold">
            See How It Works
          </a>
        </div>

        <div class="mt-10 flex flex-wrap gap-x-6 gap-y-2 text-sm text-base-content/50">
          <span class="flex items-center gap-1.5">
            <.icon name="hero-bolt-mini" class="size-4 text-warning" />
            Sub-ms latency
          </span>
          <span class="flex items-center gap-1.5">
            <.icon name="hero-chat-bubble-left-right-mini" class="size-4 text-accent" />
            Multi-agent consensus
          </span>
          <span class="flex items-center gap-1.5">
            <.icon name="hero-eye-slash-mini" class="size-4 text-info" />
            Zero data leaks
          </span>
        </div>
      </div>

      <%!-- Right: Globe canvas --%>
      <div class="relative flex items-center justify-center animate-slide-in-right">
        <div class="absolute inset-0 flex items-center justify-center pointer-events-none">
          <div class="w-64 h-64 sm:w-80 sm:h-80 lg:w-96 lg:h-96 rounded-full bg-primary/5 animate-pulse-glow"></div>
        </div>
        <div class="relative w-full aspect-square max-w-[500px]">
          <canvas id="globe-canvas" class="w-full h-full"></canvas>
        </div>
      </div>
    </div>
  </div>
</section>

<%!-- ============================================================
     STATS BAR
     ============================================================ --%>
<section class="relative border-y-[0.5px] border-base-300 bg-base-200/50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10 sm:py-14">
    <div class="grid grid-cols-2 lg:grid-cols-4 gap-8 sm:gap-12 text-center">
      <div class="reveal-on-scroll stagger-1">
        <div class="text-3xl sm:text-4xl font-extrabold text-base-content">
          &lt;<span data-counter="0.5" data-suffix="ms">0</span>
        </div>
        <div class="mt-2 text-sm text-base-content/60 font-medium">Policy Engine Latency</div>
      </div>
      <div class="reveal-on-scroll stagger-2">
        <div class="text-3xl sm:text-4xl font-extrabold text-base-content">
          <span data-counter="5" data-suffix="+">0</span>
        </div>
        <div class="mt-2 text-sm text-base-content/60 font-medium">AI Agents per Deliberation</div>
      </div>
      <div class="reveal-on-scroll stagger-3">
        <div class="text-3xl sm:text-4xl font-extrabold text-base-content">
          Real-time
        </div>
        <div class="mt-2 text-sm text-base-content/60 font-medium">Event Verdicts</div>
      </div>
      <div class="reveal-on-scroll stagger-4">
        <div class="text-3xl sm:text-4xl font-extrabold text-base-content">
          Zero
        </div>
        <div class="mt-2 text-sm text-base-content/60 font-medium">Data Leaks (GhostProtocol)</div>
      </div>
    </div>
  </div>
</section>

<%!-- ============================================================
     FEATURES SECTION
     ============================================================ --%>
<section id="features" class="scroll-mt-20 py-20 sm:py-28">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-2xl mx-auto mb-16 reveal-on-scroll">
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-info/10 border-[0.5px] border-info/20 mb-4">
        <span class="text-xs font-semibold text-info tracking-wide uppercase">Core Capabilities</span>
      </div>
      <h2 class="text-3xl sm:text-4xl font-bold tracking-tight">
        Everything you need to secure AI agents
      </h2>
      <p class="mt-4 text-base-content/60 text-lg">
        Four layers of protection working together in real-time to keep your autonomous agents safe and compliant.
      </p>
    </div>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
      <%!-- Feature 1: Policy Engine --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-6 reveal-on-scroll stagger-1">
        <div class="flex items-center justify-center size-12 rounded-xl bg-warning/10 mb-5">
          <.icon name="hero-shield-exclamation" class="size-6 text-warning" />
        </div>
        <h3 class="text-lg font-semibold mb-2">Policy Engine</h3>
        <p class="text-sm text-base-content/60 leading-relaxed">
          ETS-cached rule evaluation in sub-millisecond time. Define allow/flag/block rules that
          execute before your agents can act.
        </p>
      </div>

      <%!-- Feature 2: Multi-Agent Deliberation --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-6 reveal-on-scroll stagger-2">
        <div class="flex items-center justify-center size-12 rounded-xl bg-accent/10 mb-5">
          <.icon name="hero-chat-bubble-left-right" class="size-6 text-accent" />
        </div>
        <h3 class="text-lg font-semibold mb-2">Multi-Agent Deliberation</h3>
        <p class="text-sm text-base-content/60 leading-relaxed">
          When rules flag an event, multiple Claude agents analyze, debate, and reach consensus
          on the correct verdict.
        </p>
      </div>

      <%!-- Feature 3: GhostProtocol --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-6 reveal-on-scroll stagger-3">
        <div class="flex items-center justify-center size-12 rounded-xl bg-info/10 mb-5">
          <.icon name="hero-eye-slash" class="size-6 text-info" />
        </div>
        <h3 class="text-lg font-semibold mb-2">GhostProtocol</h3>
        <p class="text-sm text-base-content/60 leading-relaxed">
          Ephemeral agent lifecycle with cryptographic shredding. When a session ends, all
          trace data is permanently destroyed.
        </p>
      </div>

      <%!-- Feature 4: Real-Time Dashboard --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-6 reveal-on-scroll stagger-4">
        <div class="flex items-center justify-center size-12 rounded-xl bg-success/10 mb-5">
          <.icon name="hero-squares-2x2" class="size-6 text-success" />
        </div>
        <h3 class="text-lg font-semibold mb-2">Real-Time Dashboard</h3>
        <p class="text-sm text-base-content/60 leading-relaxed">
          Live event streams powered by Phoenix PubSub. See every agent action, every verdict,
          every deliberation as it happens.
        </p>
      </div>
    </div>
  </div>
</section>

<%!-- ============================================================
     HOW IT WORKS
     ============================================================ --%>
<section id="how-it-works" class="scroll-mt-20 py-20 sm:py-28 bg-base-200/30 dot-grid-bg">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-2xl mx-auto mb-16 reveal-on-scroll">
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-success/10 border-[0.5px] border-success/20 mb-4">
        <span class="text-xs font-semibold text-success tracking-wide uppercase">Simple Integration</span>
      </div>
      <h2 class="text-3xl sm:text-4xl font-bold tracking-tight">
        Three steps to AI agent security
      </h2>
      <p class="mt-4 text-base-content/60 text-lg">
        Get your AI agents protected in minutes, not weeks.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 lg:gap-12 relative">
      <%!-- Connecting line (desktop only) --%>
      <div class="hidden md:block absolute top-6 left-[16.67%] right-[16.67%] h-[0.5px] bg-base-300"></div>

      <%!-- Step 1 --%>
      <div class="relative text-center reveal-on-scroll stagger-1">
        <div class="flex items-center justify-center size-12 rounded-full bg-primary text-primary-content font-bold text-lg mx-auto mb-6 relative z-10">
          1
        </div>
        <h3 class="text-xl font-semibold mb-3">Connect</h3>
        <p class="text-base-content/60 leading-relaxed">
          Register your AI agents via our REST API. Each agent gets a unique identity and
          security profile within your workspace.
        </p>
      </div>

      <%!-- Step 2 --%>
      <div class="relative text-center reveal-on-scroll stagger-2">
        <div class="flex items-center justify-center size-12 rounded-full bg-primary text-primary-content font-bold text-lg mx-auto mb-6 relative z-10">
          2
        </div>
        <h3 class="text-xl font-semibold mb-3">Monitor</h3>
        <p class="text-base-content/60 leading-relaxed">
          Every agent action flows through the policy engine. Rules evaluate in sub-milliseconds,
          allowing, flagging, or blocking in real-time.
        </p>
      </div>

      <%!-- Step 3 --%>
      <div class="relative text-center reveal-on-scroll stagger-3">
        <div class="flex items-center justify-center size-12 rounded-full bg-primary text-primary-content font-bold text-lg mx-auto mb-6 relative z-10">
          3
        </div>
        <h3 class="text-xl font-semibold mb-3">Protect</h3>
        <p class="text-base-content/60 leading-relaxed">
          Flagged events trigger multi-agent deliberation. Multiple AI agents analyze the threat,
          debate, and deliver an auditable verdict.
        </p>
      </div>
    </div>
  </div>
</section>

<%!-- ============================================================
     PRICING
     ============================================================ --%>
<section id="pricing" class="scroll-mt-20 py-20 sm:py-28">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center max-w-2xl mx-auto mb-16 reveal-on-scroll">
      <div class="inline-flex items-center gap-2 px-3 py-1.5 rounded-full bg-accent/10 border-[0.5px] border-accent/20 mb-4">
        <span class="text-xs font-semibold text-accent tracking-wide uppercase">Pricing</span>
      </div>
      <h2 class="text-3xl sm:text-4xl font-bold tracking-tight">
        Start free, scale as you grow
      </h2>
      <p class="mt-4 text-base-content/60 text-lg">
        No credit card required. Upgrade when your AI agents need more power.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 lg:gap-8 max-w-5xl mx-auto">
      <%!-- Starter --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-8 reveal-on-scroll stagger-1">
        <div class="mb-6">
          <h3 class="text-lg font-semibold">Starter</h3>
          <div class="mt-3 flex items-baseline gap-1">
            <span class="text-4xl font-extrabold">Free</span>
          </div>
          <p class="mt-2 text-sm text-base-content/60">Perfect for experimenting</p>
        </div>
        <ul class="space-y-3 mb-8">
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            1 workspace
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            3 registered agents
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Basic policy rules
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            7-day event history
          </li>
        </ul>
        <a href={~p"/users/register"} class="btn btn-outline w-full h-[44px]">
          Get Started
        </a>
      </div>

      <%!-- Pro (highlighted) --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-primary/40 rounded-xl p-8 relative reveal-on-scroll stagger-2">
        <div class="absolute -top-3 left-1/2 -translate-x-1/2">
          <span class="inline-flex items-center px-3 py-1 rounded-full bg-primary text-primary-content text-xs font-semibold">
            Most Popular
          </span>
        </div>
        <div class="mb-6">
          <h3 class="text-lg font-semibold">Pro</h3>
          <div class="mt-3 flex items-baseline gap-1">
            <span class="text-4xl font-extrabold">$49</span>
            <span class="text-base-content/60 text-sm">/month</span>
          </div>
          <p class="mt-2 text-sm text-base-content/60">For teams shipping AI products</p>
        </div>
        <ul class="space-y-3 mb-8">
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            5 workspaces
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Unlimited agents
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Full multi-agent deliberation
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            GhostProtocol sessions
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            90-day audit history
          </li>
        </ul>
        <a href={~p"/users/register"} class="btn btn-primary w-full h-[44px]">
          Start Free Trial
        </a>
      </div>

      <%!-- Enterprise --%>
      <div class="landing-card bg-base-100 border-[0.5px] border-base-300 rounded-xl p-8 reveal-on-scroll stagger-3">
        <div class="mb-6">
          <h3 class="text-lg font-semibold">Enterprise</h3>
          <div class="mt-3 flex items-baseline gap-1">
            <span class="text-4xl font-extrabold">Custom</span>
          </div>
          <p class="mt-2 text-sm text-base-content/60">For security-critical deployments</p>
        </div>
        <ul class="space-y-3 mb-8">
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Unlimited workspaces
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Dedicated infrastructure
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Custom integrations
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            SLA guarantees
          </li>
          <li class="flex items-center gap-2.5 text-sm text-base-content/80">
            <.icon name="hero-check-mini" class="size-5 text-success shrink-0" />
            Unlimited audit history
          </li>
        </ul>
        <a href="mailto:hello@swarmshield.dev" class="btn btn-outline w-full h-[44px]">
          Contact Sales
        </a>
      </div>
    </div>
  </div>
</section>

<%!-- ============================================================
     BOTTOM CTA
     ============================================================ --%>
<section class="py-20 sm:py-28 bg-base-200/30">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8 text-center reveal-on-scroll">
    <h2 class="text-3xl sm:text-4xl font-bold tracking-tight">
      Secure your AI agents today
    </h2>
    <p class="mt-4 text-lg text-base-content/60 max-w-xl mx-auto">
      Join teams building the next generation of autonomous AI with confidence.
      Start for free, no credit card required.
    </p>
    <div class="mt-8 flex flex-col sm:flex-row gap-4 justify-center">
      <a href={~p"/users/register"} class="btn btn-primary h-[48px] px-10 text-base font-semibold">
        Get Started Free
        <.icon name="hero-arrow-right-mini" class="size-5 ml-1" />
      </a>
    </div>
  </div>
</section>

<%!-- ============================================================
     FOOTER
     ============================================================ --%>
<footer class="border-t-[0.5px] border-base-300 py-10 pb-24 md:pb-10">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex flex-col sm:flex-row items-center justify-between gap-4">
      <div class="flex items-center gap-2.5">
        <div class="flex items-center justify-center size-8 rounded-lg bg-primary/15">
          <.icon name="hero-shield-check-solid" class="size-4 text-primary" />
        </div>
        <span class="text-sm font-semibold">SwarmShield</span>
      </div>
      <div class="flex items-center gap-6 text-sm text-base-content/50">
        <a href="#features" class="hover:text-base-content transition-colors">Features</a>
        <a href="#pricing" class="hover:text-base-content transition-colors">Pricing</a>
        <a href={~p"/users/log-in"} class="hover:text-base-content transition-colors">Log in</a>
      </div>
      <p class="text-xs text-base-content/40">
        Built with Phoenix &amp; Claude
      </p>
    </div>
  </div>
</footer>

<%!-- ============================================================
     MOBILE BOTTOM NAV (md and below)
     ============================================================ --%>
<nav class="md:hidden fixed bottom-0 left-0 right-0 h-16 bg-base-100 border-t-[0.5px] border-base-300 z-50">
  <div class="grid grid-cols-5 h-full">
    <a href="#" class="flex flex-col items-center justify-center gap-1 text-primary">
      <.icon name="hero-home" class="size-5" />
      <span class="text-xs">Home</span>
    </a>
    <a href="#features" class="flex flex-col items-center justify-center gap-1 text-base-content/60">
      <.icon name="hero-shield-exclamation" class="size-5" />
      <span class="text-xs">Features</span>
    </a>
    <a href="#pricing" class="flex flex-col items-center justify-center gap-1 text-base-content/60">
      <.icon name="hero-currency-dollar" class="size-5" />
      <span class="text-xs">Pricing</span>
    </a>
    <a href={~p"/users/log-in"} class="flex flex-col items-center justify-center gap-1 text-base-content/60">
      <.icon name="hero-arrow-right-on-rectangle" class="size-5" />
      <span class="text-xs">Log in</span>
    </a>
    <a href={~p"/users/register"} class="flex flex-col items-center justify-center gap-1 text-primary">
      <.icon name="hero-rocket-launch" class="size-5" />
      <span class="text-xs font-semibold">Start</span>
    </a>
  </div>
</nav>
